# 表示時エフェクトのプレビュー

name-name の画面効果（ブルーム/ハレーション/オーバーレイ階調化等、name-name #316）を実装する前に、
theo(いませお)の立ち絵 + shadow-library 背景で ImageMagick 静止画モックアップを作り、方向性を検討した記録を置く。

各回のプレビューは日付フォルダに入れる。実装後は実機のライブ確認に置き換える（静止画は方向性検討用）。
